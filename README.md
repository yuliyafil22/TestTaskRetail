Для выполнения задания использовался Claude Code и Chat GPT 4.1.

Промпт на загрузку заказов в RetailCRM:

Сгенерируй Python-скрипт для Paycharm, который загружает заказы из файла mock_orders.json в RetailCRM через API.
Учитывай, что нужно избегать дублирующих заказов по телефону клиента.
Используй requests и dotenv для переменных окружения.

Промпт на создание SQL скрипта на создание таблицы вручную на Supabase:

Сгенерируй SQL скрипт для создания таблицы retailcrm_orders в Supabase, чтобы она подходила под структуру заказов из RetailCRM.


Промпт RetailCRM → Supabase:

Напиши Python-скрипт для Paycharm, который получает все заказы из RetailCRM через API и записывает их в таблицу retailcrm_orders в Supabase через REST API.
Нужно реализовать проверку, чтобы не было дублирующих заказов, и удалять устаревшие заказы из Supabase, которых больше нет в RetailCRM.

Ошибки и доработки
Промпт:

В скрипте синхронизации RetailCRM → Supabase возникает ошибка при обработке customFields.
Исправь код, чтобы customFields корректно преобразовывались в словарь, независимо от типа (list или dict).

Уточнения и объяснения
Промпт:

Объясни, как реализовать удаление устаревших заказов из Supabase, если их больше нет в RetailCRM.
Приведи пример кода для массового удаления по списку crm_id.


Промпт на создание скрипта Дашборда для Paycharm:

Сгенерируй код для Next.js страницы для Paycharm, которая строит график количества заказов по дням на основе данных из Supabase (таблица retailcrm_orders).
Используй Chart.js или аналогичную библиотеку для визуализации.
Покажи пример запроса к Supabase и обработку данных для графика.

Промпт Telegram-бот:

Напиши Python-скрипт для Paycharm, который раз в минуту проверяет новые заказы в RetailCRM и отправляет уведомление в Telegram, если сумма заказа больше 50,000 ₸.
Используй requests и dotenv для переменных окружения.
Сообщение должно содержать ФИО, телефон, сумму и ID заказа.


<img width="975" height="509" alt="image" src="https://github.com/user-attachments/assets/c3a4d909-9002-4347-a6c4-81d3789667cd" />

